[project]
name = "calculator2docs"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "GeertD", email = "geert2dierick@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "zensical>=0.0.55",
]

[project.scripts]
calculator2docs = "calculator2docs:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
